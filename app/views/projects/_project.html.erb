<tr id="project_<%= project.id %>"> 
  <td><%= link_to project.name, edit_project_path(project), remote: true %></td>

  <td><%= link_to "Löschen", project, :class => "btn btn-danger  btn-sm", remote: true, method: :delete, data: { confirm: 'Willst du wirklich das Projekt löschen?' } %></td>
</tr>